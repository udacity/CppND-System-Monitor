{
    "tasks": [
        {
            "type": "shell",
            "label": "g++ build project",
            "command": "/usr/bin/g++",
            "args": [
                "-g",
                "-std=c++17",
                "main.cpp",
                "-lncurses"
            ],
            "options": {
                "cwd": "${workspaceFolder}"
            }
        }

    ],
    "version": "2.0.0"
}